= link:google-apps.html[Google Apps] / Gmail =
:title: [Google Apps] Gmail

Gmail 預設會在 `http://mail.google.com/a/YOURDOMAIN.com` 服務，如果要自訂網址（`http://mail.YOURDOMAIN.com`），就必須要在 DNS 增加一筆 CNAME record 指向 `ghs.googlehosted.com` 才行。

== 延伸閱讀 ==

 * link:google-apps-dns.html[DNS 設定]

