= link:commands.html[Git / Commands] / `git branch` =
:title: [Git] git branch

常見的用法有：

 * `git branch [-r|-a]` - 預設只會列出 local branches，加上 `-r` 則只會列出 remote-tracking branches。如果要同時列出 local 跟 remote-tracking branches 則要改用 `-a`。

== 參考資料 ==

 * http://git-scm.com/docs/git-branch[git-branch(1)]

