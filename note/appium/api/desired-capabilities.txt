= link:api.html[Appium / API] / Desired Capabilities =
:title: [Appium] Desired Capabilities

// 這裡列出所有支援的 desired capabilities，分別標示是否只剩用特定平台，以及接受的值為何，格式有沒有特別的要求。

 * `app` - `apk` 的位置，不一定要給。
 * `app-packaage`
 * `app-activity` - launch activity，例如 `.MainActivity` 或 `your.package.MainActivity`
 * `device` - 可以是 `ios`、`android` 或 `selendroid`。
 * `name` -
 * `app-wait-activity` - 

// 如何指定 device?? 但又要套用 selendroid mode??
// 拿到實際支援的 capabilities 時裡面應該含有 device ID?? 

== 參考資料 ==

 * https://groups.google.com/forum/#!topic/appium-discuss/wx7EyY5FIa0/discussion[Waiting for app's activity to become focused - Google Groups] (2013-07-04)

