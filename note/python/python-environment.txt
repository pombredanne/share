= Python: 判斷執行環境並與它互動 =

NOTE: 為了讓程式可以跨平台執行，目錄分隔字元（例如 Windows 的 `\` 或 Linux 的 `/`）用 `os.sep` 取代，多個路徑的分隔字元（path separator，例如 Windows 的 `;` 或 Linux 的 `:`）則用 `os.pathsep` 取代。

其他相關的議題有：

 * link:python-environment-os.html[判斷作業系統別]
   ** link:PythonEnvOSWindows.html[判斷 Windows 的版本]
 * link:python-environment-os-bit.html[判斷作業系統的位元數]
 * link:python-environment-variable.html[存取環境變數]
 * link:python-environment-locale.html[取得系統預設的語系]

