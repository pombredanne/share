= link:virtualbox.html[VirtualBox] / VBoxManage =
:title: [VirtualBox] VBoxManage

 * 在 Linux 下，`/usr/bin/VBoxManage` `/usr/bin/vboxmanage` 都是指向 `/usr/bin/VBox`。
 * `VBoxManage list vms` 可以列出所有 VMs 的名稱跟 UUID。
 * `vboxmanage clonehd <source_vdi> <destination_vdi>` （這個動作相當耗時，最後會顯示新的 UUID）

