= link:concepts.html[Ant / Concepts] / Targets =
:title: [Ant] Targets

 * Default target - 由 `<project default="...">` 指定的 target。
 * Main targets - 帶有 `description` 的 `<target>`。
 * Other targets - 沒有 `description` 的 `<target>`。

