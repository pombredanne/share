= link:python.html[GAE / Python] / Logging =
:title: [GAE][Python] Logging

--------------------------------------------------------------------------------
import logging

logging.basicConfig(level=logging.DEBUG) # <1>
_logger = logging.getLogger(__name__)

--------------------------------------------------------------------------------
<1> 這一行沒有作用，看來 root logger 早被設定過了。

== 參考資料 ==

.文件
--
 * https://developers.google.com/appengine/articles/logging[Logging Events in Your Application - Google App Engine — Google Developers] (2012-08-21)
////////////////////////////////////////////////////////////////////////////////
** Google App Engine uses the Python logging module to allow you to log events that take place in your application. The logs that have been generated by your application are viewable under that application in the Administration Console.
** We also include all requests made to your application in our logging console.
** 試了 `if __name__ == '__main__': main()`  的做法行不通，因為 __name__ 是 module 的名稱。
** 把 `logging.basicConfig(level=logging.DEBUG)` 擺在最上面也沒用，看來 root logger 早被設定過了。
////////////////////////////////////////////////////////////////////////////////
 * https://developers.google.com/appengine/docs/python/runtime#App_Caching[Python Runtime Environment > App Caching - Google App Engine — Google Developers] ^https://diigo.com/0ydb2[閱讀]^
////////////////////////////////////////////////////////////////////////////////
** Since WSGI handlers are modules, they are cached between requests. CGI handler scripts are only cached if they provide a main() routine; otherwise, the CGI handler script is loaded for every request. 看起來 main() 的用法都是針對 CGI handler script，那 WSGI 呢？
////////////////////////////////////////////////////////////////////////////////
--

.其他文件
--
 * http://stackoverflow.com/questions/2982959/getting-logging-debug-to-work-on-google-app-engine-python[debugging - Getting logging.debug() to work on Google App Engine/Python - Stack Overflow] (2010-06-06)
////////////////////////////////////////////////////////////////////////////////
** I believe the only way to get debug messages to show up in the dev server is to pass --debug.
////////////////////////////////////////////////////////////////////////////////
--

////////////////////////////////////////////////////////////////////////////////

 * 可以確定的是 default level = INFO，但 logging level 要在哪裡做調整??

////////////////////////////////////////////////////////////////////////////////

