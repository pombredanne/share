= link:filesystem.html[Unix-like / Filesystem] / Disk Usage =
:title: [Unx] Disk Usage

要查看整個磁碟的用量，可以用 `df`：

 $ df -h

如果要查看個別資料夾的大小，可以用 `du`：

 * `du -sh` - 統計所在目錄的大小。
 * `du -h --max-depth=1` - 列出每個子目錄所佔用的空間。

