= link:bash.html[Bash] / If =
:title: [Bash] If

--------------------------------------------------------------------------------
if [ ! condition ]; then
  commands
fi
--------------------------------------------------------------------------------

 * condition = `-f`, `-eq` ...
 * `-e` 可以用來判斷是否存在，不管它的型態是什麼。

--------------------------------------------------------------------------------
if [ -e "$BASEDIR/$BASENAME.files" -a ! -e /tmp/$BASENAME.files ]; then
--------------------------------------------------------------------------------

